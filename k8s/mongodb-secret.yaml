apiVersion: v1
kind: Secret
metadata:
  name: mongodb-secret
type: Opaque
data:
  # These are base64 encoded values
  MONGO_ROOT_USERNAME: adsaoisjdoiaadsl     # 'admin' in base64
  MONGO_ROOT_PASSWORD: comlmoijeowijfnd  # 'password' in base64 